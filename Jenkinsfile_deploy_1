pipeline {
    agent any 
    stages {
      
        
        stage('---Deployment---') { 
            environment {
                SERVER_CREDS = credentials('Server-Credentials')
            }
            steps {
                echo $SERVER_CREDS_PSW
                echo $SERVER_CREDS_USR
            }
        }
             
    }
}
